interface:
  display_name: "Codex Thread Handoff"
  short_description: "Start, recover, or message peer tasks safely"
  default_prompt: "Use $codex-thread-handoff only for the explicit legacy native peer-thread route, with one bounded operation and no retries."
