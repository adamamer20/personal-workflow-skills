interface:
  display_name: "Execute Milestone"
  short_description: "Own one planned milestone end-to-end"
  default_prompt: "Use $execute-milestone to implement exactly one typed capsule, verify observable gates, and return one durable result."
