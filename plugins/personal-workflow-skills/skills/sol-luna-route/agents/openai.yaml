interface:
  display_name: "Sol-Luna Route"
  short_description: "Sol decides while a Luna commits checkpoints"
  default_prompt: "Use $sol-luna-route to execute substantial work with Sol deciding boundaries, Luna workers operating, and one Luna Git Committer creating atomic checkpoints."
